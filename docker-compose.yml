version: '3.8'
services:
  phantasm:
    build: ./backend
    ports: ["8000:8000"]
    environment:
      - ANTHROPIC_API_KEY=${ANTHROPIC_API_KEY}
    volumes:
      - ./frontend:/frontend
    restart: unless-stopped
